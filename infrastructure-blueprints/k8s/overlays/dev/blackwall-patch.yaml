apiVersion: apps/v1
kind: Deployment
metadata:
  name: blackwall
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: blackwall
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "250m"
              memory: "512Mi"
