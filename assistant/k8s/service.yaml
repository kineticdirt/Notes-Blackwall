apiVersion: v1
kind: Service
metadata:
  name: assistant
  namespace: default
  labels:
    app: assistant
spec:
  type: ClusterIP
  ports:
    - port: 8765
      targetPort: 8765
      protocol: TCP
      name: http
  selector:
    app: assistant
